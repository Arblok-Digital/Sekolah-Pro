# RAG + LANGGRAPH COMBINED CONFIG
# Node mapping: RETRIEVE (RAG) -> AUGMENT (RAG + dependency) -> GENERATE (RAG) -> VERIFY (LangGraph)
# Source: sekolah-pro/app.js (237305 bytes)
# Base backup: sekolah-pro/app.js-pre-split-backup
# Modules in folder: 10
# Constraint: CSS/index.html tidak diubah (checksum: a04d2e575a99)
# Test target: localhost:3005
